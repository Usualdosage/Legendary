@model UserModel

<div class="console-container">
    <div id="console">
        <p class="loader">One moment, we are loading the game...</p>
    </div>
    <div id="input">
        <input type="text" id="inputField" placeholder="Type message and press enter." />
    </div>
</div>

@section Scripts {
    <script>$(function () {
        const client = new LegacyClient();
        client.Connect();
    });</script>
}